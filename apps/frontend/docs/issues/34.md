# #34 — Set up Cypress config and initial E2E specs

## Summary
`apps/frontend/package.json` depends on `cypress` and has a `test:e2e` script (`cypress run`), and the README documents E2E tests living in `cypress/e2e/` — but no `cypress.config.ts` or `cypress/` directory exists anywhere in the repo.

## Scope
- Add `apps/frontend/cypress.config.ts` with a sensible `baseUrl` (local dev server) and TypeScript support.
- Add a first smoke-test spec (e.g. login page loads, dashboard requires auth) as a template for future E2E coverage.
- Add the mock-service-worker handlers referenced in the README (`mocks/handlers.ts`) if they don't exist yet, or update the README if that plan changed.

## Acceptance criteria
- [ ] `yarn workspace @truestub/frontend test:e2e` runs at least one real passing spec.
